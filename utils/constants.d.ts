export declare const CONNECTION_MODES: {
    RELAY_ONLY: string;
};
declare const ConnectionModes: string[];
export type ConnectionModes = (typeof ConnectionModes)[number];
export declare const NAT_SERVICES_LINKS: {
    TWILIO: string;
};
export declare const CONNECTION_STATUS: {
    CONNECTED: string;
    FAILED: string;
    STOPPED: string;
};
export {};
